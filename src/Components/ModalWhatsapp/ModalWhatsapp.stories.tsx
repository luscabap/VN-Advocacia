import { Meta, StoryObj } from "@storybook/react";
import { ModalWhatsApp } from "./index";

const meta = {
  title: "UIComponents/ModalWhatsApp",
  component: ModalWhatsApp
} satisfies Meta<typeof ModalWhatsApp>;

export default meta;

type Story = StoryObj<typeof meta>

export const Example: Story = {

}